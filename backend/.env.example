# Environment variables for TubeMateX
# Copy this file to .env and fill in your own credentials.
# IMPORTANT: Do NOT commit the .env file to version control.

# A long, random string used to sign the session ID cookie.
# You can generate one here: https://www.random.org/strings/
SESSION_SECRET=your_super_long_and_random_session_secret

# Google OAuth 2.0 Credentials
# Get these from the Google Cloud Platform console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# The base URL of your application.
# For local development, this will likely be http://localhost:3000
# For production, this will be your public domain name.
# IMPORTANT: Make sure the redirect URI in your Google Cloud credentials is set to
# ${BASE_URL}/auth/google/callback
BASE_URL=http://localhost:3000
